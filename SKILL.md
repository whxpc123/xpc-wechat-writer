---
name: xpc-wechat-writer
description: 为 XPC 按固定流程策划、研究、写作并交付面向中国普通用户的 AI 公众号长文。只要用户提到继续下一篇、公众号选题、选第几个题、继续同批第二个题、提供某个候选的大纲、确认大纲、写这个选题、去 AI 味、选择公众号排版主题、生成公众号 HTML、复制正文图片、标题摘要或 2.35:1 封面，就应使用本 Skill，即使用户只说「1」「确认」「继续选题 2」。它负责保存每批 5 个候选及其写作状态，允许多个候选逐篇完成；同时执行热点研究和 HKR 选题、两次确认、可选 gzh-design 主题排版、非冒充式卡兹克风格写作、真实案例与口径核查、原创配图、封面、手机电脑自适应复制工作台，并为每篇文章建立独立目录。不要用于小红书短帖、微博短文、纯下载公众号文章或自动发布。
---

# XPC 公众号文章生产线

把本 Skill 当作一条有状态的编辑流水线，而不是一次性写作提示词。先从对话和现有文章目录判断用户处在哪一步，只完成当前步骤，不重复询问已经确认的选择。

## 固定账号方向

账号定位是「中国普通人的 AI 使用实验和现实生存指南」。

长期内容比例

- 50% 工作、收入、小生意和个人经营
- 30% 豆包、千问、DeepSeek 等国内产品的真实任务测试
- 20% 家庭、教育、医疗、消费和社会议题

选题、系列排序和用户价值判断需要阅读 [references/editorial-strategy.md](references/editorial-strategy.md)。

正文写作前阅读 [references/writing-style.md](references/writing-style.md)。

创建目录、图片和 HTML 前阅读 [references/delivery-contract.md](references/delivery-contract.md)。

确定排版主题和生成公众号 HTML 前阅读 [references/gzh-layout-integration.md](references/gzh-layout-integration.md)。

生成一批选题、切换同批候选或接收用户提供的大纲时，阅读 [references/topic-batch-state.md](references/topic-batch-state.md)，并使用 `scripts/topic_batch.mjs` 维护跨会话状态。

## 默认位置

按以下顺序确定文章目录

1. 用户在当前对话中明确指定的位置
2. Skill 根目录中未被版本控制的 `config.local.md`
3. 环境变量 `XPC_WECHAT_ARTICLES_DIR`
4. 当前工作区下的 `公众号文章/`

若 `config.local.md` 存在，在创建文章目录前读取它，但不要把这个本地配置提交到公开仓库。

每篇文章使用独立目录

`YYYY-MM-DD-topic-slug/`

若当日同名目录已存在，追加简短序号或时间，不覆盖旧文章。

## 状态判断

按照最近一次有效输入判断阶段。

| 用户输入 | 当前动作 |
|---|---|
| 「继续下一篇」「给我选题」「最近写什么」 | 进入选题阶段 |
| 「1」「选题 1」「写第二个」 | 锁定候选，进入目标、资料和大纲阶段 |
| 「继续选题 2」「把第二个的大纲给我」 | 从当前选题批次恢复候选 2，进入目标、资料和大纲阶段 |
| 「这是选题 2 的大纲，按它写」并附大纲 | 绑定当前批次候选 2；视为正式大纲已确认，进入完整制作阶段 |
| 给出自定义标题或主题 | 直接进入目标、资料和大纲阶段 |
| 「确认大纲」「确认」「按默认配置」 | 进入完整制作阶段 |
| 「确认大纲，排版用石墨极简风」或其它主题名 | 确认大纲并记录该文章的排版主题，进入完整制作阶段 |
| 「用 gzh-design 排版这篇文章」 | 已指定主题或说按推荐主题时直接排；否则先给推荐项和全部可选主题 |
| 「开始写作」「开始写这篇文章」「按这个写」回复或引用已经输出的正式大纲 | 视为正式大纲已确认，直接进入完整制作阶段，不重新生成大纲 |
| 指出复制、图片、署名或排版问题 | 修复当前文章，不重新选题 |

只有选题和正式大纲是强制确认点。排版主题允许用户逐篇选择，但不新增强制确认点：在正式大纲中给出推荐项和全部主题，用户可在确认大纲时一起指定；只回复「确认」或「按默认配置」时使用本篇推荐主题。

同一批候选彼此独立。完成候选 1 后，不得把候选 2 当作全新无来源主题，也不得沿用候选 1 的正文、研究文件或文章目录。当前批次在 5 个候选全部完成、跳过，或用户明确要求开始新一批之前一直有效。

## 聊天入口的进度可见性

通过飞书等聊天入口执行需要工具或文件生成的任务时：

- 开始时先发送一条 `已接单：正在……` 回执。
- 完整制作按资料核查、正文与去 AI 味、图片与封面、HTML 与 QA 四个阶段播报；只在阶段真实完成时发送，最终交付即第四阶段完成通知。
- 短任务只保留接单回执和最终结果。真实阻塞要立即说明，不能长时间无声。
- 不展示思维过程，不逐字播报，不发送虚假百分比，不重复发送最终结果。

若宿主平台已有心跳或工具进度功能，优先使用可原地更新的单条状态消息，避免刷屏。

## 阶段一 生成 5 个选题

先研究最近 7 至 30 天中国用户真正关心的变化。热点、价格、产品能力、政策和公开数据会变化，必须联网核实。

优先来源

1. 政府、公司、产品官方公告和研究原文
2. 新华社、人民网等权威媒体
3. 有明确作者和采访对象的主流媒体
4. 社交平台只用于发现情绪和问题，不单独支撑事实

输出恰好 5 个选题。每个选题都包含

- 标题
- 文章主题
- 目标读者
- 解决的问题
- 读完获得
- 为什么现在写，并附直接来源
- HKR 分数，H、K、R 各 1 至 5 分
- 推荐文章原型
- 事实或标题风险

至少 3 个题目贴近中国普通人的收入、工作、小店经营、家庭或教育。不要连续推荐同一类国外公司发布会新闻。

明确推荐一个长期价值最高的选题，并说明它在内容序列中的位置。然后停止，等待用户回复 1 至 5。

不要在这一阶段写正文、生成图片或创建文章目录。

输出 5 个选题后立即保存选题批次状态。批次记录不属于正式文章目录，因此不违反上条约束。保存完整的 5 个候选，而不只是标题，并把它设为当前批次。最终回复补一句：以后可直接发送「继续选题 2」逐篇生产本批其他候选。

## 阶段二 目标、资料和大纲

用户选中题目后，完成前四步。

先从当前批次恢复完整候选信息，并把该候选标记为 `outline_pending`。如果用户引用的是旧批次、标题而不是编号，或存在多个可能匹配的候选，先列出匹配项让用户选择，不要猜测。

### 选题确认

输出

- 暂定标题
- 文章主题
- 目标读者
- 解决的问题
- HKR
- 文章原型

### 文章目标

明确读者看完得到的认知、方法和当天可以执行的动作。不要只写「了解某件事」。

### 资料收集

联网搜集

- 官方文件或研究原文
- 至少 2 个可核实案例
- 关键数据与统计口径
- 支持观点的反例或限制
- 可视化机会

把「报告说了什么」和「作者由此做出的判断」分开。找不到样本量、调查方法或原始文档时，明确写出限制，不要假装已经核实。

### 正式大纲

先给出核心判断，再按阅读推进设计开头、主体、方法和回环结尾。每一部分写清楚

- 解决哪个问题
- 使用哪项事实或案例
- 如何扣回主线
- 读者获得什么动作

同时给出 3 至 5 张正文图和封面的视觉计划，并增加「排版主题」：依据题材推荐 1 个 `gzh-design` 主题，列出全部可选主题及其适用场景。此时不生成图片或 HTML。

最后明确请求用户确认大纲，然后停止。不要提前创建最终 HTML。

把正式大纲保存到该候选的批次状态目录。若用户自己提供了完整大纲：

- 仅说「给选题 2 做大纲」时，生成大纲并等待确认。
- 明确说「按这个大纲写」「直接写」或同义表达时，把用户提供的大纲视为已经确认，不再要求重复确认。
- 用户回复或引用本助手已经输出的正式大纲并说「开始写作」「开始写这篇文章」「按这个写」时，同样视为确认；引用内容已经包含完整大纲，不得误判为只有选题已确认。
- 大纲明显缺少核心判断、主体结构或结尾动作时，指出最少的必要补充；不因措辞或格式差异阻塞写作。

## 阶段三 完整制作

收到确认后持续执行到交付完成，中间只在真实阻塞或需要改变已确认方向时询问。

开始制作前把候选状态更新为 `producing`。完成验收后更新为 `completed`，记录文章目录、最终标题、`article.html` 和 `qa-report.md` 路径。然后列出本批仍为 `candidate` 或 `outline_pending` 的候选编号，方便用户继续写第二篇、第三篇。

### 1 研究落盘

创建文章目录并保存

- `spec.md`，确认后的主题、读者、目标、结构和视觉
- `research.md`，来源链接、关键数据、案例和口径限制

所有时效性事实在写作当天再次核对。医疗、法律、财务和政策内容需要更高标准的官方来源与风险提示。

### 2 写正文

优先调用以下可用 Skill，并在使用前读取各自说明

1. `content-research-writer` 或 `huashu-research`，补齐证据
2. `khazix-writer`，借用叙事节奏和现象解读方法
3. `humanize-chinese`，做最小必要的去 AI 味修改

借用风格不等于冒充作者。不得声称是卡兹克，不得加入他的署名、邮箱或固定投稿尾部，也不得编造第一人称体验。

先保存 `article-draft.md`，去 AI 味和事实复核后保存 `article.md`。

### 3 生成正文图

默认生成 5 张正文图，1600 × 900 PNG。

默认视觉

- 类型按内容使用 infographic、comparison、flowchart、framework
- 风格 `sketch-notes`
- 配色 `macaron`
- 暖奶油纸张、黑色手绘线、柔和色块
- 简体中文、短标签、无水印、无品牌 Logo

使用 `baoyu-article-illustrator` 和原生 `imagegen`。每张图必须先保存提示词到 `imgs/prompts/`，再生成。

生成后逐张查看。若文字错误、擅自增加案例细节、出现英文杂字或把抽象品类画成未经证实的具体产品，保存新的修正版提示词并重新生成。不得用 Pillow、Canvas、SVG 或位图覆盖修补文字。

### 4 生成封面

默认封面

- 1800 × 766 PNG
- 比例 2.35 比 1
- conceptual 类型
- cool 配色
- digital 渲染
- title-only
- clean 字体
- balanced 情绪
- 无水印和 Logo

使用 `baoyu-cover-image` 和原生 `imagegen`。封面标题必须与最终标题逐字一致，标点也要检查。允许只做裁切和缩放，不允许程序覆盖标题。

### 5 去 AI 味终审

对照写作参考执行四层检查

- 硬规则，禁用词、标点、署名和虚构经验
- 节奏，短段落、疑问、断裂句和回环
- 内容，观点有证据，限制写清楚，方法能执行
- 活人感，像有判断的普通人在认真聊天，不像报告或品牌稿

去 AI 味只做最小必要改写，不能把数据、引用和限定条件改掉。

### 6 排版和转换

调用 `baoyu-format-markdown` 时保留原有散文结构，只做中文与英文间距、强调符号和图片引用整理。不要自动添加大量小标题。

按照 [references/gzh-layout-integration.md](references/gzh-layout-integration.md) 定位并调用 `gzh-design`。完整阅读其 `SKILL.md`、主题索引、用户选中的主题组件库和通用组件库，再生成：

- `article-formatted.md`，保留最终正文结构
- `article-formatted.html`，供工作台使用的纯 `<section>` 公众号正文
- `article_排版_{主题中文名}({英文标识}).html`，便于识别主题的同内容副本

主题不能固定为摸鱼绿。优先使用用户在确认大纲时指定的主题；用户未指定时使用正式大纲中的推荐项。不得静默退回旧的 `modern + blue` 排版。

运行 `gzh-design/scripts/validate_gzh_html.py article-formatted.html`，必须达到 0 ERROR、0 WARNING 后才能构建工作台。

### 7 构建复制工作台

使用本 Skill 的脚本

```bash
node <skill-dir>/scripts/build_publish.mjs <article-dir>
```

它会生成

- `article.html`，主要交付页面
- `publish.html`，备用入口
- `wechat-body.html`，纯正文页面

工作台必须提供四个按钮

- 复制标题
- 复制摘要
- 复制正文和全部图片
- 复制封面

正文和封面图片需要以内嵌 PNG 数据加入工作台，确保从 `file://` 打开也能复制，不能只依赖相对路径。

### 8 验收

先运行

```bash
node <skill-dir>/scripts/verify_delivery.mjs <article-dir>
```

工具可用时，再用 Playwright 或浏览器完成

- 从 `file://` 打开 `article.html`
- 点击四个复制按钮
- 确认状态全部成功
- 截取 1440 像素桌面端和 390 像素手机端页面
- 检查控制台错误

保存 `qa-report.md`。若任何复制、图片或口径检查失败，修复并重新构建，不要把半成品交给用户。

## 最终交付

最终回复只突出结果

- `article.html` 的可点击绝对路径
- `article.md` 的可点击绝对路径
- `qa-report.md` 的可点击绝对路径
- 简短说明复制测试、图片数量和封面比例
- 可选展示封面预览

不要自动发布到公众号，不要替用户发送外部消息。

## 用户纠错的长期规则

这些是已经确认过的偏好，不得回退

- 不出现 `/ 作者，卡兹克`
- 不出现投稿或爆料邮箱
- 不虚构作者亲测
- 正文和图片必须一起复制成功
- 标题、摘要和封面可以单独复制
- 每篇文章必须有独立目录
- 选题和正式大纲必须等用户确认
- 排版主题可逐篇选择；只确认大纲而未指定主题时使用本篇推荐项
